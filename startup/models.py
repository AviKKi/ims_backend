from django.db import models

# Create your models here.
# class Application(AbstractUser):
#     User=models.CharField(blank=False,)
#     Document=models.CharField(blank=True,max_length=200 )
#     MeetingDate=models.DateTimeField(blank=False,max_length=20)
#     Approved=models.Boolean(Boolean)
#     Approved_At=models.Boolean(Boolean)
